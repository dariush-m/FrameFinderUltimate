import java.util.List;

public class BLAST {
    protected List<String> blastORFs(List<ORF> ORFs){
        // Run the BLAST search and return the results in XML format
        // Maybe make a separate thread.
        return List.of("");
    }
}
